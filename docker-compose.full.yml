services:
  backend:
    build: .
    restart: always
    environment:
      DATABASE_URL: "file:./dev.db"
      JWT_SECRET: supersecretkey
      PORT: 4000
      HOST: 0.0.0.0
    volumes:
      - ./dev.db:/app/dev.db
    networks:
      - easy-panel

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: "http://backend:4000"
    restart: always
    depends_on:
      - backend
    networks:
      - easy-panel

networks:
  easy-panel:
    external: true